# Configuring-PAgP-LACP-Static-Etherchannels
An etherChannel bundles multiple physical network links into a single, high-bandwidth logical link. It increases bandwidth and provides fault tolerance through redundancy, as traffic is distributed across the physical links. By grouping links, EtherChannel also prevents the Spanning Tree Protocol (STP) from blocking redundant paths.
